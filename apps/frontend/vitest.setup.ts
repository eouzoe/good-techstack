import "@testing-library/jest-dom/vitest";

// Strict mode surfaces hydration mismatches (server HTML !== client HTML)
// and non-serializable props passed from RSC to client components.
// See docs/agent/testing.md for the testing contract.
